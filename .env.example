# نسخ إلى `.env` محلياً إن لزم. عند التجاهل يُستخدم الافتراضي في `src/config/apiEndpoint.js`.
#
# خيار 1 — عنوان يتضمن `/api` (مثال):
# VITE_API_URL=https://localhost:7001/api
#
# خيار 2 — host فقط بدون `/api` (يُضاف تلقائياً):
# VITE_API_BASE_URL=https://api.example.com
#
VITE_API_URL=https://trave-gdb0b6ccfecpbahv.israelcentral-01.azurewebsites.net/api
